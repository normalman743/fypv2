# 校园LLM 前端

该目录包含使用 React 和 Vite 构建的前端代码。

## 开发启动

```bash
cd frontend
npm install
npm run dev
```

开发服务器启动后可通过浏览器访问显示界面，默认连接到 `http://localhost:8000/api/v1` 后端。

可在 `.env` 文件中设置 `VITE_API_BASE_URL` 指定其他后端地址。
